{
  "name": "nowwhy-tracker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "./node_modules/.bin/esbuild src/index.ts --bundle --minify --target=es2015 --outfile=../public/tracker.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.71.2"
  }
}
